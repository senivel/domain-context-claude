---
phase: 09-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/dc/add.md
  - commands/dc/validate.md
autonomous: true
requirements:
  - ADDC-04
  - REFR-04
  - EXPL-03
  - VALD-05
gap_closure: true

must_haves:
  truths:
    - "dc:add Step 5 replaces {verified_date} token BEFORE stripping HTML comments"
    - "dc:add Step 5 strips instructional HTML comments but preserves <!-- verified: ... --> lines"
    - "dc:validate orphan registration entry formats use em dash (U+2014) not double-dash"
    - "dc:validate broken link fix creates target directories before writing files"
  artifacts:
    - path: "commands/dc/add.md"
      provides: "INT-01 fix: token replacement before comment stripping with verified comment preservation"
      contains: "Replace `{verified_date}` with today"
    - path: "commands/dc/validate.md"
      provides: "INT-02 fix: em dash in orphan entry formats; INT-03 fix: mkdir -p before file write"
      contains: "mkdir -p"
  key_links:
    - from: "commands/dc/add.md Step 5"
      to: "templates/domain-concept.md line 3"
      via: "verified_date token replacement preserves <!-- verified: YYYY-MM-DD -->"
      pattern: "verified"
    - from: "commands/dc/validate.md orphan registration"
      to: "commands/dc/add.md execution_context entry formats"
      via: "em dash format consistency"
      pattern: "\\u2014"
---

<objective>
Fix 3 minor cross-phase integration bugs (INT-01, INT-02, INT-03) in dc:add and dc:validate skill files.

Purpose: Restore full integration quality between dc:add, dc:validate, dc:explore, and dc:refresh. All 3 bugs are surgical text edits -- no new files, libraries, or architecture changes.
Output: Two modified skill files with corrected instruction ordering (INT-01), consistent formatting (INT-02), and edge-case guard (INT-03).
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-fixes/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix INT-01 -- dc:add verified comment stripping order</name>
  <files>commands/dc/add.md</files>
  <action>
In `commands/dc/add.md`, Step 5 "Extract Title and Content", modify points 4 and 5 (currently lines 92-93).

Current text (points 4-5):
```
4. Strip all HTML comments (`<!-- ... -->`) from the filled content.
5. Replace `{verified_date}` with today's date in YYYY-MM-DD format.
```

Replace with:
```
4. Replace `{verified_date}` with today's date in YYYY-MM-DD format.
5. Strip HTML comments from the filled content, except for `<!-- verified: ... -->` lines (these are functional metadata, not guidance comments).
```

This swaps the order (token replacement first) AND updates the strip instruction to explicitly preserve the functional verified-date comment. Without both changes, the fix is incomplete -- swapping alone still destroys the comment because "strip all" includes verified comments.

Do NOT change any other lines in the file.
  </action>
  <verify>
Read `commands/dc/add.md` lines 90-95. Confirm:
1. Point 4 says "Replace `{verified_date}`" (token replacement FIRST)
2. Point 5 says "Strip HTML comments" with "except for `<!-- verified: ... -->` lines" (preservation clause)
3. No other lines changed
  </verify>
  <done>dc:add Step 5 replaces {verified_date} before stripping comments, and the strip instruction explicitly preserves verified-date comments. Files created by dc:add will retain the <!-- verified: YYYY-MM-DD --> inline comment.</done>
</task>

<task type="auto">
  <name>Task 2: Fix INT-02 and INT-03 -- dc:validate em dash format and directory guard</name>
  <files>commands/dc/validate.md</files>
  <action>
In `commands/dc/validate.md`, make two independent fixes:

**INT-02 fix (lines 62-64 in execution_context, orphan registration entry formats):**

Current text:
```
- Domain concepts: `- [{Name}]({relative-path}) -- {placeholder description} [public] [verified: {today}]`
- Decisions: `- [{NNN}: {Title}]({relative-path}) -- {placeholder description} [verified: {today}]`
- Constraints: `- [{Name}]({relative-path}) -- {placeholder description} [public] [verified: {today}]`
```

Replace `--` with the em dash character ` — ` (U+2014, with spaces on both sides) in all three lines:
```
- Domain concepts: `- [{Name}]({relative-path}) — {placeholder description} [public] [verified: {today}]`
- Decisions: `- [{NNN}: {Title}]({relative-path}) — {placeholder description} [verified: {today}]`
- Constraints: `- [{Name}]({relative-path}) — {placeholder description} [public] [verified: {today}]`
```

Do NOT change line 65 (module context format) -- it has no dash separator.

**INT-03 fix (Step 8, Broken Links "Create" fix, point 7):**

Find the numbered sub-step that currently reads:
```
  7. Write the file to the expected path (`.context/` + the entry's relative path).
  8. Record count of files created.
```

Insert a new step 7 for the directory guard, and renumber:
```
  7. Ensure the target directory exists by running `mkdir -p` on the parent directory of the file path.
  8. Write the file to the expected path (`.context/` + the entry's relative path).
  9. Record count of files created.
```

Do NOT change any other lines in the file.
  </action>
  <verify>
Read `commands/dc/validate.md` and confirm:
1. Lines 62-64: all three orphan entry formats use ` — ` (em dash), not ` -- `
2. Line 65 (module context) is unchanged
3. Step 8 Broken Links "Create" path has 9 sub-steps (was 8), with "mkdir -p" at step 7, "Write" at step 8, "Record" at step 9
  </verify>
  <done>dc:validate orphan registration uses em dash matching canonical MANIFEST.md format. Broken link fix creates target directories before writing files, preventing failures when .context/ subdirs are missing.</done>
</task>

</tasks>

<verification>
Read both modified files end-to-end and trace each fix:

1. **INT-01 trace:** In add.md Step 5, mentally process `domain-concept.md` template:
   - Point 4 replaces `{verified_date}` -> `<!-- verified: 2026-03-16 -->` survives
   - Point 5 strips instructional comments (`<!-- 2-4 sentences: ... -->`) but preserves verified comment
   - Result: created file contains `<!-- verified: 2026-03-16 -->` on line 3

2. **INT-02 trace:** In validate.md execution_context, orphan entry formats now use ` — ` matching:
   - dc:add's execution_context (lines 28-30) which already uses em dash
   - dc:validate's own entry line format documentation (line 27) which already uses em dash

3. **INT-03 trace:** In validate.md Step 8, broken link "Create" path:
   - Step 7 runs mkdir -p on parent directory
   - Step 8 writes the file (guaranteed directory exists)
   - Handles edge case where user deleted .context/ subdirs but not MANIFEST.md
</verification>

<success_criteria>
- commands/dc/add.md Step 5 replaces tokens before stripping comments, with verified comment preservation
- commands/dc/validate.md orphan registration uses em dash in all 3 entry formats
- commands/dc/validate.md broken link fix includes mkdir -p before file write
- No other lines in either file are changed
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-fixes/09-01-SUMMARY.md`
</output>
