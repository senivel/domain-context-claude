---
phase: 02-init-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [INIT-01, INIT-02, INIT-03, INIT-04, INIT-05, INIT-06]

must_haves:
  truths:
    - "Running /dc:init on a fresh project creates .context/MANIFEST.md with filled description"
    - "Running /dc:init creates .context/domain/.gitkeep, .context/decisions/.gitkeep, .context/constraints/.gitkeep"
    - "Running /dc:init creates ARCHITECTURE.md with System Purpose filled and TODOs for remaining sections"
    - "Running /dc:init creates or appends AGENTS.md with domain-context sentinel snippet"
    - "Running /dc:init creates or appends CLAUDE.md with @AGENTS.md pointer"
    - "Running /dc:init adds .context.local/ to .gitignore"
  artifacts:
    - path: ".context/MANIFEST.md"
      provides: "Filled manifest in test project"
      contains: "Context Manifest"
    - path: "ARCHITECTURE.md"
      provides: "Architecture skeleton in test project"
      contains: "System Purpose"
    - path: "AGENTS.md"
      provides: "AGENTS.md with domain-context snippet"
      contains: "domain-context:start"
  key_links:
    - from: "commands/dc/init.md"
      to: "templates/*.md"
      via: "Template resolution at runtime"
      pattern: "domain-context/templates"
---

<objective>
Verify that the dc:init skill works end-to-end by running it on a fresh temporary project and confirming all 6 success criteria from the roadmap are met.

Purpose: The skill file from Plan 01 is instructions for Claude -- this plan proves those instructions produce the correct output.
Output: Verified working skill; no new files created in the project (test is on a temp directory).
</objective>

<execution_context>
@/Users/alevine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-init-core/02-CONTEXT.md
@.planning/phases/02-init-core/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare test environment and run dc:init</name>
  <files></files>
  <action>
Create a temporary test directory to simulate a fresh project:

1. Create a temp directory: `mktemp -d /tmp/dc-init-test-XXXXX`
2. Inside it, create a minimal package.json with a "description" field so metadata inference has something to find:
   ```json
   {"name": "test-project", "description": "A test project for verifying dc:init"}
   ```
3. Initialize git: `git init` (so the commit offer can work)
4. Set up template resolution -- the skill needs to find templates. Create a symlink or copy:
   - Create `.claude/domain-context/templates/` in the temp directory
   - Copy all files from this project's `templates/` directory into it
5. Run the dc:init skill manually by reading `commands/dc/init.md` and following its process step by step on the temp directory. Execute each step as written in the skill.
6. When the skill asks about committing, choose "No" (this is a test).

Record the narration output from each step.
  </action>
  <verify>
    <automated>TESTDIR=$(ls -d /tmp/dc-init-test-* 2>/dev/null | tail -1) && test -f "$TESTDIR/.context/MANIFEST.md" && test -f "$TESTDIR/.context/domain/.gitkeep" && test -f "$TESTDIR/.context/decisions/.gitkeep" && test -f "$TESTDIR/.context/constraints/.gitkeep" && test -f "$TESTDIR/ARCHITECTURE.md" && grep -q "domain-context:start" "$TESTDIR/AGENTS.md" && grep -q "@AGENTS.md" "$TESTDIR/CLAUDE.md" && grep -q ".context.local/" "$TESTDIR/.gitignore" && echo "PASS: All 6 criteria met" || echo "FAIL: Some criteria not met"</automated>
  </verify>
  <done>
- Temp directory contains .context/MANIFEST.md with "A test project for verifying dc:init" as description
- Temp directory contains .context/domain/.gitkeep, .context/decisions/.gitkeep, .context/constraints/.gitkeep
- Temp directory contains ARCHITECTURE.md with "System Purpose" section filled
- Temp directory contains AGENTS.md with domain-context:start sentinel block
- Temp directory contains CLAUDE.md with @AGENTS.md
- Temp directory contains .gitignore with .context.local/
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies dc:init output quality</name>
  <action>
Present the test results to the user for review. Show the contents of each created file in the test directory so the user can verify quality.
  </action>
  <verify>User types "approved" or provides feedback</verify>
  <done>User confirms all created files look correct and the skill works as expected</done>
  <what-built>The dc:init skill has been created and tested on a fresh project. All 6 INIT requirements should be verified working.</what-built>
  <how-to-verify>
    1. Review the test output from Task 1 -- all 6 success criteria should pass
    2. Optionally run /dc:init yourself on any test project to see the full flow
    3. Check that created files look correct:
       - .context/MANIFEST.md has the project description filled in (no raw {placeholder} tokens)
       - ARCHITECTURE.md has System Purpose filled, other sections have TODO comments
       - AGENTS.md has the domain-context snippet with sentinel comments
       - CLAUDE.md has @AGENTS.md reference
       - .gitignore has .context.local/
       - No HTML comments remain in created files
    4. Verify the skill file itself at commands/dc/init.md reads clearly as procedural instructions
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All 6 Phase 2 success criteria from the roadmap:
1. .context/MANIFEST.md created from template with filled placeholders
2. domain/, decisions/, constraints/ subdirectories with .gitkeep files
3. ARCHITECTURE.md skeleton with System Purpose filled
4. AGENTS.md with domain-context sentinel snippet
5. CLAUDE.md with @AGENTS.md pointer
6. .gitignore with .context.local/ entry
</verification>

<success_criteria>
- All 6 INIT requirements verified working on a fresh project
- No placeholder tokens left unfilled in MANIFEST.md
- No HTML comments left in created files
- AGENTS.md injection uses sentinel pattern correctly
- User approves the skill output at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-init-core/02-02-SUMMARY.md`
</output>
