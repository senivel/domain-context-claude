---
phase: 23-conceptual-and-contributor-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/content/docs/guides/architecture.mdx
  - docs/src/content/docs/guides/spec-overview.mdx
  - docs/src/content/docs/guides/user-guide.mdx
autonomous: true
requirements: [CONT-05, CONT-06]

must_haves:
  truths:
    - "Architecture page explains the bridge pattern (CLAUDE.md -> AGENTS.md -> .context/) with a text diagram"
    - "Architecture page shows the module map table and .context/ directory tree"
    - "Architecture page describes hook lifecycle from user's perspective (freshness warnings, edit reminders)"
    - "Spec overview page explains the three pillars (domain concepts, decisions, constraints) and required directory structure"
    - "Spec overview page includes one example domain concept file structure"
    - "Spec overview page has prominent links to github.com/senivel/domain-context at top and bottom"
    - "Spec overview page positions the tool as 'the spec defines the format; this tool automates the workflow'"
  artifacts:
    - path: "docs/src/content/docs/guides/architecture.mdx"
      provides: "Architecture and concepts documentation page"
      contains: "bridge pattern"
    - path: "docs/src/content/docs/guides/spec-overview.mdx"
      provides: "Domain Context spec overview page"
      contains: "github.com/senivel/domain-context"
  key_links:
    - from: "docs/src/content/docs/guides/architecture.mdx"
      to: "/domain-context-claude/guides/user-guide/"
      via: "cross-reference link"
      pattern: "/domain-context-claude/guides/user-guide/"
    - from: "docs/src/content/docs/guides/spec-overview.mdx"
      to: "https://github.com/senivel/domain-context"
      via: "external link"
      pattern: "github.com/senivel/domain-context"
---

<objective>
Create the architecture/concepts page and spec overview page for the documentation site.

Purpose: Users need to understand the system's design (bridge pattern, hooks, .context/ structure) and the specification it implements.
Output: Two MDX pages in docs/src/content/docs/guides/ plus sidebar ordering on existing user-guide.mdx.
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-conceptual-and-contributor-content/23-CONTEXT.md
@.planning/phases/23-conceptual-and-contributor-content/23-RESEARCH.md

Source material:
@ARCHITECTURE.md
@.context/domain/integration-model.md
@.context/domain/claude-code-extensions.md
@.context/decisions/002-agents-md-bridge.md
@.context/MANIFEST.md

Existing pattern reference:
@docs/src/content/docs/guides/user-guide.mdx
@docs/astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Architecture and concepts page plus sidebar ordering</name>
  <files>docs/src/content/docs/guides/architecture.mdx, docs/src/content/docs/guides/user-guide.mdx</files>
  <action>
Create `docs/src/content/docs/guides/architecture.mdx` with frontmatter:
- title: "Architecture & Concepts"
- description: short description of bridge pattern, hooks, and .context/ structure
- sidebar: { order: 2 }

Also add `sidebar: { order: 1 }` to existing user-guide.mdx frontmatter (add the sidebar field, keep existing title/description).

Content for architecture.mdx -- follow concise/practical tone from Phase 22, use double dashes (--), Starlight admonitions where helpful:

1. **Opening paragraph**: Brief intro -- this page explains how the tool's components fit together.

2. **The bridge pattern section**: Explain the three-layer chain: CLAUDE.md imports AGENTS.md, which references ARCHITECTURE.md and .context/MANIFEST.md. Include this text diagram:
```
CLAUDE.md
  └─ @AGENTS.md
       ├─ @ARCHITECTURE.md
       └─ @.context/MANIFEST.md
            ├─ domain/
            ├─ decisions/
            └─ constraints/
```
Explain why: vendor-neutral AGENTS.md means any AI tool (not just Claude) can consume domain context. GSD agents inherit domain awareness through this chain without requiring any GSD modifications.

3. **Three concerns section**: Explain The How (AGENTS.md -- procedural instructions), The What (.planning/ -- current dev specs), The Why (.context/ -- durable domain knowledge). Brief, one sentence each.

4. **Module map section**: Reproduce a simplified module map table:
| Module | Purpose |
|--------|---------|
| commands/dc/ | Six slash commands for managing domain context |
| hooks/ | Session-start freshness check, edit-time reminders |
| agents/ | Domain validator subagent |
| rules/ | Formatting guidance for .context/ files |
| templates/ | Scaffolding files for /dc:init |
| bin/ | npm installer (npx entry point) |

5. **Hook lifecycle section**: Explain what users observe (NOT stdin/stdout internals per locked decision):
- On session start: freshness check scans MANIFEST.md verified dates, warns about stale entries
- On file edit: context reminder checks if edited file is near a CONTEXT.md, reminds to update if relevant
- Both hooks degrade gracefully -- they never block a session

6. **.context/ directory structure section**: Show annotated tree:
```
.context/
  MANIFEST.md          # Index of all domain knowledge
  domain/              # Business rules, models, terminology
  decisions/           # Architecture decision records (ADRs)
  constraints/         # External requirements, API contracts
```
Brief explanation of each directory's purpose.

7. **Cross-links**: Link to User Guide for workflow details, CLI Reference for command details, Spec Overview for the specification.

All internal links MUST include `/domain-context-claude/` base path.
  </action>
  <verify>
    <automated>cd /Users/alevine/code/domain-context-claude/docs && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>architecture.mdx renders without build errors, contains bridge pattern diagram, module map table, hook lifecycle, .context/ tree. user-guide.mdx has sidebar order 1. Both pages appear in correct sidebar position.</done>
</task>

<task type="auto">
  <name>Task 2: Spec overview page</name>
  <files>docs/src/content/docs/guides/spec-overview.mdx</files>
  <action>
Create `docs/src/content/docs/guides/spec-overview.mdx` with frontmatter:
- title: "Spec Overview"
- description: short description of the Domain Context specification
- sidebar: { order: 3 }

Content -- follow same tone as architecture page:

1. **Opening with prominent spec link**: Start with a note/callout linking to the full spec: "Domain Context is an open specification. Read the full spec at [github.com/senivel/domain-context](https://github.com/senivel/domain-context)."

2. **What the spec is section**: The Domain Context specification defines a standard format for storing domain knowledge alongside code. It is language-agnostic and tool-agnostic -- any project, any AI assistant. Position clearly: "The spec defines the format; this tool automates the workflow."

3. **Three pillars section**: Explain the three types of context the spec organizes:
- **Domain concepts** (domain/) -- business rules, models, terminology, relationships
- **Decisions** (decisions/) -- architecture decision records with rationale and tradeoffs
- **Constraints** (constraints/) -- external requirements, API contracts, regulatory needs

4. **Required directory structure section**: Show the directory structure:
```
.context/
  MANIFEST.md
  domain/
  decisions/
  constraints/
```
Brief explanation that MANIFEST.md is the index (~300 tokens to scan) that enables progressive disclosure.

5. **Example domain concept file section**: Show one brief example structure:
```markdown
# Billing Model

<!-- verified: 2026-03-15 -->

## What This Is
[Description of the domain concept]

## Key Attributes
[Business rules, lifecycle, relationships]

## Business Rules
[Invariants and constraints]
```
Explain the verified comment tracks freshness.

6. **How this tool implements the spec section**: Brief list -- /dc:init creates the structure, /dc:explore and /dc:add help populate it, /dc:validate checks correctness, /dc:refresh updates dates, /dc:extract promotes planning knowledge. Link to [CLI Reference](/domain-context-claude/reference/cli/) and [User Guide](/domain-context-claude/guides/user-guide/).

7. **Closing with prominent spec link**: End with another link to the full spec repository.

All internal links MUST include `/domain-context-claude/` base path.
  </action>
  <verify>
    <automated>cd /Users/alevine/code/domain-context-claude/docs && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>spec-overview.mdx renders without build errors, contains prominent spec links at top and bottom, three pillars section, example domain concept file, clear "spec defines format, tool automates workflow" positioning.</done>
</task>

</tasks>

<verification>
- `cd docs && npm run build` succeeds with no errors
- architecture.mdx contains bridge pattern text diagram, module map table, hook lifecycle, .context/ tree
- spec-overview.mdx contains links to github.com/senivel/domain-context at top and bottom
- user-guide.mdx has sidebar order frontmatter
- All internal links use /domain-context-claude/ base path
</verification>

<success_criteria>
- Both MDX pages build successfully in Starlight
- Architecture page covers bridge pattern, module map, hook lifecycle, directory structure
- Spec overview covers three pillars, example file, spec vs tool positioning
- Sidebar shows: User Guide (1), Architecture & Concepts (2), Spec Overview (3)
</success_criteria>

<output>
After completion, create `.planning/phases/23-conceptual-and-contributor-content/23-01-SUMMARY.md`
</output>
