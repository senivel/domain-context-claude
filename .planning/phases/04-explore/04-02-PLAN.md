---
phase: 04-explore
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - EXPL-01
  - EXPL-02
  - EXPL-03
  - EXPL-04
  - EXPL-05
  - EXPL-06

must_haves:
  truths:
    - "Running /dc:explore shows grouped summary with accurate counts"
    - "Stale entries show STALE warning with day count"
    - "Keyword search finds matching entries across name, description, and content"
    - "Running /dc:explore without .context/ suggests /dc:init"
    - "Progressive disclosure lets user drill into specific entries"
    - "Module Context Files section appears in summary"
  artifacts: []
  key_links: []
---

<objective>
Verify dc:explore works correctly by running it on this project's .context/ directory and checking all 6 requirements.

Purpose: The skill file is pure markdown instructions — the only way to verify behavior is to run `/dc:explore` in Claude Code and observe the output.
Output: Human-verified confirmation that all EXPL requirements are met.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-explore/04-CONTEXT.md
@.planning/phases/04-explore/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dc:explore on this project</name>
  <files>commands/dc/explore.md</files>
  <action>
Human verifies dc:explore behavior by running the skill in Claude Code.

What was built: dc:explore skill file that parses .context/MANIFEST.md and provides summary, freshness, keyword search, progressive disclosure, and module CONTEXT.md listing.

How to verify — run these tests in Claude Code (or a fresh Claude Code session on this project):

1. **Summary display (EXPL-01):** Run `/dc:explore`. Verify output shows grouped sections with counts: Domain Concepts (2), Architecture Decisions (3), Constraints (0), Module Context Files (0). Each entry shows name and freshness status.

2. **Freshness status (EXPL-02):** Check that entries with verified dates within 90 days show `[verified: YYYY-MM-DD]`. If any entries are older than 90 days, verify they show `[STALE - N days]`.

3. **Progressive disclosure (EXPL-05):** After the summary, verify it asks "Explore an entry?" with section options. Pick a section, pick an entry, verify full content is displayed. Verify it loops back to offer more exploration.

4. **Module Context Files (EXPL-06):** Verify Module Context Files section appears in summary (should show count 0 for this project since MANIFEST.md has no module context entries).

5. **No-context fallback (EXPL-04):** In a directory without .context/, run `/dc:explore`. Verify it displays: "No .context/ directory found. Run /dc:init to set up domain context."

6. **Keyword search (EXPL-03):** Run `/dc:explore bridge` (or another keyword matching an entry). Verify it finds and displays matching entries with match location.

Resume signal: Type "approved" if all 6 checks pass, or describe which checks failed and what the actual behavior was.
  </action>
  <verify>Human confirms all 6 EXPL requirements produce correct behavior</verify>
  <done>All 6 EXPL requirements verified: summary with counts, freshness status, keyword search, no-context fallback, progressive disclosure, and module context listing all work as specified</done>
</task>

</tasks>

<verification>
All 6 EXPL requirements verified by human observation of `/dc:explore` running in Claude Code.
</verification>

<success_criteria>
- Human confirms all 6 EXPL requirements produce correct behavior
- Summary format matches the specified layout (grouped sections, counts, freshness)
- Progressive disclosure flow works as designed (section -> entry -> content -> loop)
- Keyword search finds entries across name, description, and content
- No-context fallback shows correct message
</success_criteria>

<output>
After completion, create `.planning/phases/04-explore/04-02-SUMMARY.md`
</output>
