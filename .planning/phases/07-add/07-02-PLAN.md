---
phase: 07-add
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified: []
autonomous: false
requirements:
  - ADDC-01
  - ADDC-02
  - ADDC-03
  - ADDC-04
  - ADDC-05
  - ADDC-06

must_haves:
  truths:
    - "Running /dc:add with no argument prompts for type selection"
    - "Running /dc:add decision auto-detects next ADR number"
    - "Freeform description is extracted into structured template sections"
    - "Created file uses kebab-case naming"
    - "MANIFEST.md contains the new entry after add completes"
    - "Preview is shown before writing"
  artifacts: []
  key_links:
    - from: "commands/dc/add.md"
      to: ".context/MANIFEST.md"
      via: "Entry insertion after file creation"
      pattern: "MANIFEST.md"
---

<objective>
Verify dc:add works end-to-end by running it on this project. Confirm type selection, freeform extraction, ADR numbering, file creation, and MANIFEST.md integration all function correctly.

Purpose: Catch issues in the skill instructions before shipping -- skill files are markdown instructions that can only be tested by execution.
Output: Human-verified confirmation that dc:add works correctly.
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-add/07-01-SUMMARY.md

@commands/dc/add.md
@.context/MANIFEST.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dc:add end-to-end</name>
  <what-built>dc:add skill file that creates domain concepts, decisions, and constraints from freeform conversation</what-built>
  <how-to-verify>
Test the following scenarios in this project:

1. **Type prompt (ADDC-02):** Run `/dc:add` with no argument. Verify it asks you to choose between Domain concept, Architecture decision, and Constraint.

2. **Domain concept creation (ADDC-01, ADDC-04, ADDC-05, ADDC-06):**
   - Choose "Domain concept"
   - When prompted for a description, provide: "The Skill Execution Model describes how Claude Code interprets and executes skill files. Skills are markdown files with YAML frontmatter that define allowed tools. Claude reads the process section and follows the steps sequentially. Each step may use tools like Read, Write, Glob, or AskUserQuestion."
   - Verify the preview shows content mapped to template sections (What This Is, Lifecycle, etc.)
   - Verify the filename is kebab-case (e.g., `skill-execution-model.md`)
   - Accept the preview
   - Verify the file is created in `.context/domain/`
   - Verify MANIFEST.md has a new entry under `## Domain Concepts` with today's date

3. **Decision creation with ADR numbering (ADDC-03):**
   - Run `/dc:add decision`
   - Describe: "We decided to use markdown skill files instead of JavaScript for Claude Code commands. Markdown is more readable and easier to maintain. The tradeoff is less programmatic control, but Claude handles the interpretation natively."
   - Verify the ADR number is 004 (since 001-003 already exist)
   - Verify the filename is `004-markdown-skill-files.md` (or similar kebab-case)
   - Accept and verify file creation + MANIFEST.md update

4. **After testing:** Revert any test files created (delete the test domain concept and decision files, revert MANIFEST.md changes) to leave the project clean.

Expected outcomes:
- Type selection works with and without argument
- Freeform text is intelligently mapped to template sections
- Files are created with correct naming
- MANIFEST.md is updated with properly formatted entry lines
- ADR numbering correctly detects next available number
  </how-to-verify>
  <resume-signal>Type "approved" if dc:add works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All 6 ADDC requirements tested (ADDC-07 private access is harder to test without creating .context.local/ -- verify by reading the skill instructions)
- Files created match template structure
- MANIFEST.md entry format is consistent with existing entries
</verification>

<success_criteria>
- User confirms dc:add works end-to-end for domain concepts and decisions
- Type selection, freeform extraction, ADR numbering, file creation, and MANIFEST.md update all verified
- No regressions to existing .context/ structure
</success_criteria>

<output>
After completion, create `.planning/phases/07-add/07-02-SUMMARY.md`
</output>
