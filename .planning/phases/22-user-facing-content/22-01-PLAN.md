---
phase: 22-user-facing-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/astro.config.mjs
  - docs/src/content/docs/index.mdx
  - docs/src/content/docs/getting-started/quickstart.mdx
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "The landing page communicates what domain-context-cc does, its value proposition, and shows the install command"
    - "The landing page links to the quickstart via a hero 'Get Started' button"
    - "A user following the quickstart goes from zero to a working .context/ directory in under 5 minutes"
    - "The sidebar shows three groups: Start Here, Guides, Reference"
  artifacts:
    - path: "docs/astro.config.mjs"
      provides: "Sidebar with three autogenerate groups"
      contains: "Start Here"
    - path: "docs/src/content/docs/index.mdx"
      provides: "Splash landing page with hero, value prop, install command, feature cards"
      contains: "template: splash"
    - path: "docs/src/content/docs/getting-started/quickstart.mdx"
      provides: "Step-by-step quickstart guide"
      contains: "Steps"
  key_links:
    - from: "docs/src/content/docs/index.mdx"
      to: "docs/src/content/docs/getting-started/quickstart.mdx"
      via: "hero action link"
      pattern: "/domain-context-claude/getting-started/quickstart/"
    - from: "docs/src/content/docs/getting-started/quickstart.mdx"
      to: "docs/src/content/docs/guides/user-guide.mdx"
      via: "next steps inline link"
      pattern: "/domain-context-claude/guides/user-guide/"
---

<objective>
Create the landing page, quickstart guide, and sidebar navigation structure for the documentation site.

Purpose: Establish the site's entry points so users can discover the project and get started immediately.
Output: Splash landing page with hero/value-prop/feature-cards, quickstart guide with Steps component, and three-group sidebar config.
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-user-facing-content/22-RESEARCH.md
@.planning/phases/22-user-facing-content/22-CONTEXT.md

Content sources (read these during execution):
@README.md
@commands/dc/init.md

<interfaces>
<!-- Current astro.config.mjs sidebar structure (to be expanded from 1 group to 3) -->
From docs/astro.config.mjs:
```javascript
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';

export default defineConfig({
  site: 'https://senivel.github.io',
  base: '/domain-context-claude',
  integrations: [
    starlight({
      title: 'Domain Context',
      social: [
        { icon: 'github', label: 'GitHub', href: 'https://github.com/senivel/domain-context-claude' },
      ],
      sidebar: [
        {
          label: 'Getting Started',
          autogenerate: { directory: 'getting-started' },
        },
      ],
    }),
  ],
});
```

<!-- Current index.mdx (placeholder to be replaced) -->
From docs/src/content/docs/index.mdx:
```yaml
---
title: Domain Context for Claude Code
description: Codify and maintain domain knowledge alongside code so AI assistants always have accurate business context.
template: splash
hero:
  tagline: Documentation site — coming soon
  actions:
    - text: GitHub
      link: https://github.com/senivel/domain-context-claude
      icon: external
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sidebar config and create landing page</name>
  <files>docs/astro.config.mjs, docs/src/content/docs/index.mdx</files>
  <action>
**1. Update docs/astro.config.mjs sidebar** to have three groups:

```javascript
sidebar: [
  {
    label: 'Start Here',
    autogenerate: { directory: 'getting-started' },
  },
  {
    label: 'Guides',
    autogenerate: { directory: 'guides' },
  },
  {
    label: 'Reference',
    autogenerate: { directory: 'reference' },
  },
],
```

Change the label from 'Getting Started' to 'Start Here' per user decision. Add Guides and Reference groups. Keep all other config (site, base, social, title) unchanged.

**2. Rewrite docs/src/content/docs/index.mdx** as a full splash landing page:

Frontmatter:
- title: "Domain Context for Claude Code"
- description: "Codify and maintain domain knowledge alongside code so AI assistants always have accurate business context."
- template: splash
- hero.tagline: "Make Claude Code natively aware of your project's domain knowledge."
- hero.actions: two buttons — "Get Started" (primary, right-arrow icon, links to `/domain-context-claude/getting-started/quickstart/`) and "View on GitHub" (minimal, external icon, links to `https://github.com/senivel/domain-context-claude`)

Body content after frontmatter:
- Import `Card` and `CardGrid` from `@astrojs/starlight/components`
- H2 "Install" with a single code block showing `npx domain-context-cc`
- H2 "What you get" with a CardGrid containing 6 cards (one per command):
  - "Initialize" (icon: "pencil") — "Scaffold `.context/` directories, wire AGENTS.md and CLAUDE.md."
  - "Explore" (icon: "open-book") — "Browse domain knowledge with freshness tracking and keyword search."
  - "Validate" (icon: "approve-check-circle") — "Detect broken links, orphan files, and stale entries."
  - "Add" (icon: "add-document") — "Create domain concepts, architecture decisions, and constraints."
  - "Refresh" (icon: "seti:clock") — "Review stale entries and update them against current code."
  - "Extract" (icon: "rocket") — "Promote knowledge from GSD planning artifacts into permanent context."
- H2 "What gets installed" with a brief list (5 items from README's "What Gets Installed" section): Skills, Hooks, Agent, Rules, Templates — each as a bold term followed by a dash and short description
- H2 "Links" with an inline link to the [Domain Context Specification](https://github.com/senivel/domain-context) and [GSD](https://github.com/cyanheads/get-shit-done-cc)

Source content from: README.md first paragraph, Commands table, "What Gets Installed" section.
Tone: Concise, practical, action-oriented. Short sentences.
All internal links must include `/domain-context-claude/` base path.

Note on icons: Use well-known Starlight icon names. If "add-document" or "seti:clock" don't render, fall back to simpler alternatives like "document" and "setting" respectively — verify in dev build output. The build will warn about unknown icons but will not fail.
  </action>
  <verify>
    <automated>cd /Users/alevine/code/domain-context-claude/docs && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- astro.config.mjs has three sidebar groups: "Start Here", "Guides", "Reference"
- index.mdx renders as splash page with hero, two action buttons, install command, 6 feature cards, "What gets installed" list, and links
- Build succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create quickstart guide</name>
  <files>docs/src/content/docs/getting-started/quickstart.mdx</files>
  <action>
Create `docs/src/content/docs/getting-started/quickstart.mdx`.

Frontmatter:
- title: "Quickstart"
- description: "Go from zero to a working .context/ directory in under 5 minutes."

Body content:
- Import `Steps` from `@astrojs/starlight/components`
- H2 "Prerequisites" with a bullet list: "Node.js 20+" and "[Claude Code](https://claude.ai/code)"
- H2 "Install and initialize" with a `<Steps>` component containing 3 numbered steps:
  1. "Install domain-context-cc globally:" with code block `npx domain-context-cc` — add a note aside (`:::note`) below mentioning local install variant: "For project-specific install, use `npx domain-context-cc --local` instead."
  2. "Open your project in Claude Code and run:" with code block `/dc:init` — follow with a brief description: "This creates the `.context/` directory with MANIFEST.md, domain/, decisions/, and constraints/ subdirectories. It also scaffolds ARCHITECTURE.md, wires AGENTS.md, and creates CLAUDE.md."
  3. "Start working — Claude Code is now domain-context-aware." — brief sentence: "Domain context hooks will remind you to keep `.context/` files up to date as you work."
- H2 "What just happened" — brief paragraph explaining what dc:init created (source from README dc:init paragraph and commands/dc/init.md objective). Show example output in a code block:
```
Domain Context initialized:
  .context/MANIFEST.md     created
  .context/domain/         created
  .context/decisions/      created
  .context/constraints/    created
  ARCHITECTURE.md          created
  AGENTS.md                created
  CLAUDE.md                created
  .gitignore               updated
```
- H2 "Next steps" with 3 bullet points linking to:
  - [User Guide](/domain-context-claude/guides/user-guide/) — "Learn the full workflow: explore, validate, add, refresh, extract"
  - [CLI Reference](/domain-context-claude/reference/cli/) — "Detailed usage for all 6 commands"
  - [Domain Context Spec](https://github.com/senivel/domain-context) — "The specification this tool implements"

Source content from: README.md Quick Start section, commands/dc/init.md objective and process steps.
Tone: Concise, practical, action-oriented. Short sentences.
All internal links must include `/domain-context-claude/` base path.
  </action>
  <verify>
    <automated>cd /Users/alevine/code/domain-context-claude/docs && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
- quickstart.mdx exists in getting-started/ directory
- Page renders with Steps component, prerequisites, install steps, example output, and next steps links
- Build succeeds without errors
- Page appears in "Start Here" sidebar group
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd docs && npm run build` succeeds
2. Sidebar has three groups visible in the build output structure
3. Landing page and quickstart page render (verify by checking dist/ output)
</verification>

<success_criteria>
- Landing page communicates value prop, shows install command, links to quickstart
- Quickstart guide walks through install + init in 3 steps with example output
- Sidebar configured with three groups: Start Here, Guides, Reference
- All internal links use /domain-context-claude/ base path
- Astro build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/22-user-facing-content/22-01-SUMMARY.md`
</output>
