---
phase: 08-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified: []
autonomous: false
requirements:
  - REFR-01
  - REFR-02
  - REFR-03
  - REFR-04
  - REFR-05

must_haves:
  truths:
    - "dc:refresh correctly identifies stale entries in a real project"
    - "dc:refresh shows relevant code alongside entry content"
    - "Bumping a date updates both MANIFEST.md and the context file"
    - "Clean state message displays correctly when no entries are stale"
  artifacts: []
  key_links: []
---

<objective>
Verify dc:refresh works end-to-end on this project's own .context/ directory. Test the stale entry detection, code-aware assessment, date bumping, and clean-state message.

Purpose: Confirm the skill works in a real-world scenario before shipping.
Output: Human-verified confirmation that all REFR requirements are met.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-refresh/08-01-SUMMARY.md
</execution_context>

<context>
@commands/dc/refresh.md
@.context/MANIFEST.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dc:refresh end-to-end</name>
  <files>commands/dc/refresh.md</files>
  <action>
Run /dc:refresh in this project (domain-context-claude) and verify all REFR requirements work correctly.

What was built: dc:refresh skill file that reviews stale domain context entries with code-aware assessment, per-entry confirmation, date bumping (dual location), and content diff proposals.

How to verify:
1. Run `/dc:refresh` in this project.
2. Observe: Does it parse MANIFEST.md and identify stale entries (or report all fresh)?
3. If stale entries exist:
   - Verify it shows entry content alongside relevant source code snippets.
   - Verify it provides an accuracy assessment with reasoning.
   - Verify it presents per-entry options (bump date / edit first / skip).
   - Accept "bump date" on one entry. Check that both MANIFEST.md and the context file have updated verified dates.
4. If no stale entries: verify the "All entries are fresh. N entries checked, none older than 90 days." message displays correctly.
5. Verify the commit prompt appears after processing (if changes were made).
  </action>
  <verify>Human confirms all 5 REFR requirements are met via manual invocation.</verify>
  <done>User types "approved" confirming dc:refresh works correctly.</done>
</task>

</tasks>

<verification>
All 5 REFR requirements confirmed working via manual invocation:
- REFR-01: Skill invocable as /dc:refresh
- REFR-02: Stale entries identified (>90 days or missing date)
- REFR-03: Entry shown alongside relevant source code
- REFR-04: Date bumped in both MANIFEST.md and context file
- REFR-05: Content diffs proposed for drifted entries (if applicable)
</verification>

<success_criteria>
- Human confirms dc:refresh works correctly on real .context/ data
- No errors or unexpected behavior during the workflow
</success_criteria>

<output>
After completion, create `.planning/phases/08-refresh/08-02-SUMMARY.md`
</output>
