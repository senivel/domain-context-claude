---
phase: 15-dc-init-gsd-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [commands/dc/init.md]
autonomous: true
requirements: [BRIDGE-02]

must_haves:
  truths:
    - "Running dc:init on a project with .planning/PROJECT.md injects GSD bridge snippet into AGENTS.md"
    - "Re-running dc:init on an existing project replaces GSD bridge content between sentinels without duplication"
    - "Running dc:init on a project without .planning/PROJECT.md prompts user and skips GSD bridge if user declines"
    - "Step 10 summary shows AGENTS.md (GSD) as 9th tracked item with created/skipped/updated status"
  artifacts:
    - path: "commands/dc/init.md"
      provides: "Updated dc:init skill with Step 7.5 GSD bridge injection"
      contains: "Step 7.5"
  key_links:
    - from: "commands/dc/init.md (Step 7.5)"
      to: "templates/gsd-agents-snippet.md"
      via: "TEMPLATE_DIR read reference"
      pattern: "gsd-agents-snippet.md"
    - from: "commands/dc/init.md (Step 7.5)"
      to: "AGENTS.md (target project)"
      via: "sentinel-based injection/replacement"
      pattern: "gsd-bridge:start"
---

<objective>
Add GSD bridge detection and injection to the dc:init skill. Insert Step 7.5 that checks for .planning/PROJECT.md, prompts user if absent, and injects/updates the GSD bridge snippet in AGENTS.md using sentinel markers. Update execution_context, status tracking count, and Step 10 summary format.

Purpose: Fulfills BRIDGE-02 -- re-running dc:init updates GSD bridge content via sentinel replacement.
Output: Modified commands/dc/init.md with complete GSD bridge integration.
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-dc-init-gsd-detection/15-CONTEXT.md
@.planning/phases/15-dc-init-gsd-detection/15-RESEARCH.md

<interfaces>
<!-- The existing skill file and template the executor needs to understand -->

From commands/dc/init.md (current structure):
- YAML frontmatter with allowed-tools including AskUserQuestion
- <execution_context> lists 4 template files (manifest.md, architecture.md, agents-snippet.md, claude.md)
- <process> has Steps 1-10
- Step 1 mentions "8 items to track"
- Step 7 handles AGENTS.md injection with create/skip/update pattern using <!-- domain-context:start/end --> sentinels
- Step 10 shows 8-item summary with counts

From templates/gsd-agents-snippet.md (14 lines):
- Starts with <!-- gsd-bridge:start -->
- Ends with <!-- gsd-bridge:end -->
- Contains GSD integration instructions referencing .planning/PROJECT.md, .planning/STATE.md, /dc:extract
- Static content, no placeholders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 7.5 GSD bridge injection to dc:init skill</name>
  <files>commands/dc/init.md</files>
  <action>
Modify commands/dc/init.md with these four changes:

1. **execution_context section** -- Add after the `agents-snippet.md` bullet:
   ```
   - `gsd-agents-snippet.md` -- GSD bridge snippet with `<!-- gsd-bridge:start/end -->` sentinels
   ```

2. **Step 1 status tracking** -- Change "The 8 items to track are:" to "The 9 items to track are:" and add `AGENTS.md (GSD)` to the list (after `AGENTS.md`).

3. **Insert Step 7.5 between Step 7 and Step 8** -- Add this new step:

   ```markdown
   ## Step 7.5: Inject GSD Bridge Snippet

   1. Read `gsd-agents-snippet.md` from TEMPLATE_DIR.
   2. Check if `.planning/PROJECT.md` exists in the project root.
   3. If `.planning/PROJECT.md` does not exist:
      - Ask the user using AskUserQuestion:
        - Prompt: "This project doesn't have GSD set up yet. Add GSD bridge to AGENTS.md anyway?"
        - Options:
          1. "Yes" -- continue to inject GSD bridge
          2. "No" -- Record status `AGENTS.md (GSD): skipped` and move to the next step
   4. Read `AGENTS.md` from the project root (it exists -- Step 7 ensured this).
   5. If `AGENTS.md` contains `<!-- gsd-bridge:start -->`:
      - Replace everything from `<!-- gsd-bridge:start -->` through `<!-- gsd-bridge:end -->` (inclusive) with the fresh template content.
      - Record status `AGENTS.md (GSD): updated`.
   6. If `AGENTS.md` does not contain `<!-- gsd-bridge:start -->`:
      - Read the existing content.
      - Write the existing content followed by two blank lines and the GSD bridge snippet.
      - Record status `AGENTS.md (GSD): created`.
   ```

   Do NOT renumber Steps 8, 9, 10. Use "Step 7.5" exactly as specified.

4. **Step 10 summary format** -- Update the example display to include `AGENTS.md (GSD)` immediately after the `AGENTS.md` line:
   ```
     AGENTS.md                updated
     AGENTS.md (GSD)          created
   ```
   Update the item count references: change "8 items total" to "9 items total" and adjust the example count line to reflect 9 items (e.g., "5 created, 2 skipped, 2 updated").

Important constraints:
- Do NOT merge GSD logic into Step 7. Step 7 handles domain-context snippet, Step 7.5 handles GSD bridge -- these are independent concerns per user decision.
- Do NOT embed template content inline. Step 7.5 must READ gsd-agents-snippet.md from TEMPLATE_DIR.
- Two blank lines separate GSD bridge from existing AGENTS.md content (matches Step 7's append pattern).
- If user previously had GSD bridge but removed GSD, leave existing bridge text (sentinel replacement handles updates, not removal).
  </action>
  <verify>
    <automated>bash tools/validate-templates.sh . && grep -q "Step 7.5" commands/dc/init.md && grep -q "gsd-agents-snippet.md" commands/dc/init.md && grep -q "9 items" commands/dc/init.md && grep -q "AGENTS.md (GSD)" commands/dc/init.md && echo "All checks passed"</automated>
  </verify>
  <done>
  - commands/dc/init.md contains Step 7.5 with full GSD bridge injection logic
  - execution_context references gsd-agents-snippet.md
  - Step 1 tracks 9 items including AGENTS.md (GSD)
  - Step 10 summary shows AGENTS.md (GSD) as 9th item after AGENTS.md
  - Steps 8, 9, 10 are NOT renumbered
  - Template validation passes
  </done>
</task>

</tasks>

<verification>
1. `bash tools/validate-templates.sh .` -- all template validations pass
2. `grep -c "Step 7.5" commands/dc/init.md` returns 1+ (step exists)
3. `grep "gsd-agents-snippet" commands/dc/init.md` shows reference in execution_context and Step 7.5
4. `grep "9 items" commands/dc/init.md` confirms updated count
5. `grep "AGENTS.md (GSD)" commands/dc/init.md` shows presence in status tracking and Step 10 summary
6. `grep -c "Step 8\|Step 9\|Step 10" commands/dc/init.md` confirms original step numbers preserved
</verification>

<success_criteria>
- dc:init skill file contains complete Step 7.5 with GSD detection, user prompt, sentinel injection, and sentinel replacement logic
- All 4 modification points updated (execution_context, Step 1 count, Step 7.5, Step 10 summary)
- No existing steps renumbered
- Template validation passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-dc-init-gsd-detection/15-01-SUMMARY.md`
</output>
