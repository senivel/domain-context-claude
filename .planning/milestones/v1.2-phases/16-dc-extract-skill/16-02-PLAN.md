---
phase: 16-dc-extract-skill
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified: []
autonomous: false
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - PROP-02
  - PROP-03

must_haves:
  truths:
    - "Running /dc:extract against this project's own .planning/ phases produces proposals"
    - "The skill handles missing .context/ or .planning/ gracefully with clear errors"
    - "The proposal table displays correctly with source attribution"
    - "The accept/reject flow works via AskUserQuestion"
  artifacts: []
  key_links:
    - from: "commands/dc/extract.md"
      to: ".planning/phases/"
      via: "Scans for completed phases with SUMMARY.md"
      pattern: "SUMMARY.md"
---

<objective>
Verify dc:extract works end-to-end against this project's own planning artifacts.

Purpose: The skill file is markdown instructions — the only way to validate it works is to run it. This checkpoint confirms the scan-classify-propose-accept flow functions correctly.

Output: Human-verified working skill
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dc-extract-skill/16-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dc:extract end-to-end flow</name>
  <action>
Human verifies the dc:extract skill works correctly by running it against this project's own planning artifacts. The skill file was created in Plan 01 — this checkpoint validates it produces correct behavior when Claude interprets the instructions.
  </action>
  <verify>
    1. Run `/dc:extract` in this project (which has completed phases 14 and 15)
    2. Verify it finds and scans completed phases (those with SUMMARY.md files)
    3. Verify proposals are presented in a summary table with type, title, and source
    4. Verify you can accept/reject individual proposals
    5. Verify accepted proposals create files in .context/ and update MANIFEST.md
    6. Test error handling: temporarily rename .planning/ and run `/dc:extract` — confirm clear error message
    7. Test phase scoping: run `/dc:extract 14` — confirm only phase 14 is scanned
  </verify>
  <done>User confirms dc:extract works end-to-end: scanning, classification, preview, accept/reject, file creation, and summary all function correctly</done>
</task>

</tasks>

<verification>
Human confirms the end-to-end flow works: scan -> classify -> preview -> accept/reject -> create files -> summary
</verification>

<success_criteria>
- /dc:extract produces sensible proposals from this project's own .planning/ phases
- Error messages display correctly when prerequisites are missing
- Phase scoping filters correctly
- Accept/reject flow functions via AskUserQuestion
- Created files are spec-compliant and MANIFEST.md is updated correctly
</success_criteria>

<output>
After completion, create `.planning/phases/16-dc-extract-skill/16-02-SUMMARY.md`
</output>
