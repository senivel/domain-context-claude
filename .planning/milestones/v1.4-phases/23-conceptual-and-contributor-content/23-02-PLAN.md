---
phase: 23-conceptual-and-contributor-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/src/content/docs/guides/contributing.mdx
autonomous: true
requirements: [CONT-07]

must_haves:
  truths:
    - "Contributing guide has step-by-step local dev setup instructions"
    - "Contributing guide lists code conventions for skills, hooks, templates, and naming"
    - "Contributing guide describes the PR process (fork, branch, imperative commits)"
    - "Contributing guide includes a directory listing showing what each top-level dir contains"
  artifacts:
    - path: "docs/src/content/docs/guides/contributing.mdx"
      provides: "Contributing guide for new contributors"
      contains: "node bin/install.js --local"
  key_links:
    - from: "docs/src/content/docs/guides/contributing.mdx"
      to: "/domain-context-claude/reference/cli/"
      via: "cross-reference link"
      pattern: "/domain-context-claude/reference/cli/"
---

<objective>
Create the contributing guide for the documentation site.

Purpose: Contributors need clear instructions for local development setup, code conventions, and the PR process to participate effectively.
Output: One MDX page in docs/src/content/docs/guides/.
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-conceptual-and-contributor-content/23-CONTEXT.md
@.planning/phases/23-conceptual-and-contributor-content/23-RESEARCH.md

Source material:
@AGENTS.md
@README.md
@ARCHITECTURE.md

Existing pattern reference:
@docs/src/content/docs/guides/user-guide.mdx
@docs/astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contributing guide page</name>
  <files>docs/src/content/docs/guides/contributing.mdx</files>
  <action>
Create `docs/src/content/docs/guides/contributing.mdx` with frontmatter:
- title: "Contributing"
- description: short description of how to set up, develop, and contribute
- sidebar: { order: 4 }

Content -- follow concise/practical tone from Phase 22, double dashes, Starlight admonitions where useful:

1. **Opening paragraph**: Brief welcome -- contributions are welcome, here's how to get started.

2. **Local development setup section**: Step-by-step using Starlight Steps component if it fits naturally (import from `@astrojs/starlight/components`), otherwise numbered list:

```bash
git clone https://github.com/senivel/domain-context-claude.git
cd domain-context-claude
node bin/install.js --local
bash tools/validate-context.sh .
npm pack && npx ./domain-context-cc-*.tgz
```

Explain each step briefly: clone the repo, install skills/hooks locally into .claude/, run the context validator, test the full npm package flow.

3. **Project structure section**: Brief directory listing showing what each top-level dir contains:
```
commands/dc/    Six slash commands (/dc:init, /dc:explore, etc.)
hooks/          Session-start and edit-time lifecycle hooks
agents/         Domain validator subagent
rules/          Path-specific editing guidance for .context/ files
templates/      Scaffolding files copied by /dc:init
tools/          Standalone validation script
bin/            npm installer entry point
docs/           Documentation site (Astro/Starlight)
```

4. **Code conventions section**: Cover each file type's format:
- **Skills**: Markdown with YAML frontmatter (name, description, allowed-tools) plus objective/execution_context/process sections
- **Hooks**: Node.js scripts reading JSON from stdin, writing JSON to stdout. Must be timeout-safe, must exit 0 on error.
- **Templates**: Plain markdown with {placeholder} tokens
- **Naming**: kebab-case for all files, dc: prefix for all skill names
- **Dependencies**: No runtime dependencies -- Node.js built-ins only

5. **Pull request process section**: Brief and practical:
- Fork the repository and branch from main
- Use imperative mood commit messages (e.g., "add freshness check hook", not "added" or "adding")
- Context changes (.context/ files) go through the same review process as code
- Open a PR with a clear description of what changed and why

6. **Cross-links**: Link to Architecture & Concepts for understanding the system design, CLI Reference for command details.

All internal links MUST include `/domain-context-claude/` base path.
  </action>
  <verify>
    <automated>cd /Users/alevine/code/domain-context-claude/docs && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>contributing.mdx renders without build errors, contains local dev setup steps, project structure directory listing, code conventions for all file types, PR process instructions. Appears as 4th item in Guides sidebar.</done>
</task>

</tasks>

<verification>
- `cd docs && npm run build` succeeds with no errors
- contributing.mdx contains clone/install/validate/pack steps
- contributing.mdx contains directory listing with purpose annotations
- contributing.mdx contains code conventions for skills, hooks, templates
- All internal links use /domain-context-claude/ base path
</verification>

<success_criteria>
- Contributing page builds successfully in Starlight
- A new contributor can follow the setup steps from clone to working local install
- Code conventions section covers all four file types (skills, hooks, templates, rules)
- Sidebar shows Contributing as 4th item in Guides group
</success_criteria>

<output>
After completion, create `.planning/phases/23-conceptual-and-contributor-content/23-02-SUMMARY.md`
</output>
