---
phase: 20-scaffold-starlight-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/package.json
  - docs/astro.config.mjs
  - docs/tsconfig.json
  - docs/src/content.config.ts
  - docs/src/content/docs/index.mdx
  - .gitignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04

must_haves:
  truths:
    - "Running npm run dev inside docs/ serves a Starlight site on localhost with a placeholder page"
    - "Sidebar navigation, full-text search, dark/light toggle, code highlighting with copy, and responsive layout all work using framework defaults"
    - "docs/ has its own package.json and the root package.json is unchanged"
    - "npm pack --dry-run from project root excludes docs/"
  artifacts:
    - path: "docs/package.json"
      provides: "Isolated Astro/Starlight dependencies"
      contains: "@astrojs/starlight"
    - path: "docs/astro.config.mjs"
      provides: "Starlight integration config"
      contains: "starlight"
    - path: "docs/src/content.config.ts"
      provides: "Content collection schema with loader"
      contains: "docsLoader"
    - path: "docs/src/content/docs/index.mdx"
      provides: "Placeholder landing page"
      contains: "Domain Context"
  key_links:
    - from: "docs/astro.config.mjs"
      to: "@astrojs/starlight"
      via: "integrations array"
      pattern: "starlight\\("
    - from: "docs/src/content.config.ts"
      to: "@astrojs/starlight/loaders"
      via: "docsLoader import"
      pattern: "docsLoader"
---

<objective>
Scaffold a complete Astro Starlight documentation site in docs/ with all framework defaults active, fully isolated from the root npm package.

Purpose: Establish the documentation infrastructure that all subsequent content phases (21-24) build upon.
Output: A working Starlight site that builds and serves locally, with Astro build artifacts gitignored and docs/ excluded from npm tarball.
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-scaffold-starlight-site/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Starlight project in docs/</name>
  <files>docs/package.json, docs/astro.config.mjs, docs/tsconfig.json, docs/src/content.config.ts, docs/src/content/docs/index.mdx</files>
  <action>
Create the docs/ directory and scaffold a Starlight project manually (do NOT use the interactive `npm create astro` wizard -- it requires interactive prompts). Create each file directly:

1. **docs/package.json** -- Minimal package.json with:
   - name: "domain-context-docs"
   - private: true
   - scripts: dev, build, preview (standard Astro scripts)
   - dependencies: astro, @astrojs/starlight (use latest versions -- astro 6.x, @astrojs/starlight 0.38.x)

2. **docs/astro.config.mjs** -- Configure Starlight integration:
   - title: "Domain Context"
   - social link to GitHub repo: https://github.com/senivel/domain-context-claude
   - sidebar with a single "Getting Started" group that autogenerates from directory
   - Do NOT add a base URL yet (that is Phase 21 for GitHub Pages)

3. **docs/tsconfig.json** -- Extend Astro's strict config:
   ```json
   { "extends": "astro/tsconfigs/strict" }
   ```

4. **docs/src/content.config.ts** -- Content collection using current Astro 5+/6 API:
   - Import docsLoader from @astrojs/starlight/loaders
   - Import docsSchema from @astrojs/starlight/schema
   - Define docs collection with loader: docsLoader() and schema: docsSchema()

5. **docs/src/content/docs/index.mdx** -- Splash landing page:
   - title: "Domain Context for Claude Code"
   - description: project tagline
   - template: splash
   - hero with tagline "Documentation site -- coming soon"
   - Single action linking to GitHub repo

After creating files, run `cd docs && npm install` to install dependencies.

IMPORTANT: The root package.json must NOT be modified. All docs dependencies live inside docs/.
  </action>
  <verify>
    <automated>cd /Users/alevine/code/domain-context-claude/docs && npm run build</automated>
  </verify>
  <done>docs/ directory contains a complete Starlight project that builds successfully. docs/node_modules/ contains astro and starlight. Root package.json is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Gitignore updates and npm pack isolation verification</name>
  <files>.gitignore</files>
  <action>
1. Add Astro-specific entries to the ROOT .gitignore file. Append these lines under a new "### Astro (docs site) ###" comment section:
   - docs/dist/
   - docs/.astro/
   - docs/node_modules/

   Note: The generic `node_modules/` pattern already in .gitignore covers docs/node_modules/, but adding the explicit entry provides clarity. The docs/dist/ and docs/.astro/ entries are essential -- they are NOT covered by existing patterns.

2. Verify npm pack exclusion by running `npm pack --dry-run` from project root and confirming no docs/ files appear in the output. The root package.json `files` whitelist (commands/, agents/, hooks/, rules/, templates/, tools/, bin/) already excludes docs/ implicitly.

3. Verify the Starlight dev server starts by running `cd docs && npm run dev` briefly (start and immediately kill) to confirm no startup errors.
  </action>
  <verify>
    <automated>cd /Users/alevine/code/domain-context-claude && npm pack --dry-run 2>&1 | grep -c "docs/" | grep -q "^0$" && echo "PASS: docs/ excluded from tarball"</automated>
  </verify>
  <done>.gitignore has Astro artifact entries. npm pack --dry-run shows zero docs/ files. Starlight build succeeds from Task 1 verification.</done>
</task>

</tasks>

<verification>
1. `cd docs && npm run build` -- site builds without errors
2. `npm pack --dry-run` from root -- no docs/ files in tarball
3. `git diff package.json` -- root package.json unchanged (no diff)
4. `test -f docs/package.json && grep -q starlight docs/package.json` -- Starlight dependency present
</verification>

<success_criteria>
- Starlight site builds successfully in docs/
- All five framework defaults are active (sidebar nav, Pagefind search, dark/light toggle, Expressive Code with copy, responsive layout) -- these require zero configuration, they ship with Starlight
- docs/ is fully isolated with own package.json and node_modules
- Root package.json has zero changes
- npm tarball excludes docs/ entirely
- .gitignore covers docs/dist/, docs/.astro/
</success_criteria>

<output>
After completion, create `.planning/phases/20-scaffold-starlight-site/20-01-SUMMARY.md`
</output>
