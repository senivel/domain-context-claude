---
phase: 01-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/manifest.md
  - templates/context.md
  - templates/domain-concept.md
  - templates/decision.md
  - templates/constraint.md
  - templates/agents-snippet.md
  - templates/architecture.md
  - templates/claude.md
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03

must_haves:
  truths:
    - "8 template files exist in templates/ directory"
    - "Each template contains every section heading required by the Domain Context spec (Section 6)"
    - "Dynamic content positions use {placeholder} tokens with snake_case names"
    - "Static content (Access Levels, AGENTS.md snippet) contains no placeholders"
    - "Guidance comments are brief HTML comments (one-line) placed below headings, above placeholders"
    - "AGENTS.md snippet is wrapped in <!-- domain-context:start --> / <!-- domain-context:end --> sentinel comments"
  artifacts:
    - path: "templates/manifest.md"
      provides: "MANIFEST.md template with Access Levels, Domain Concepts, Architecture Decisions, Constraints, Module Context Files sections"
      contains: "## Domain Concepts"
    - path: "templates/context.md"
      provides: "Module CONTEXT.md template with What This Module Does, Domain Concepts Owned, Business Rules, etc."
      contains: "## What This Module Does"
    - path: "templates/domain-concept.md"
      provides: "Domain concept template with What This Is, Lifecycle, Key Attributes, Business Rules, Invariants, Related Concepts"
      contains: "## What This Is"
    - path: "templates/decision.md"
      provides: "ADR template with Status, Context, Decision, Rationale, Consequences, Affected Modules"
      contains: "## Rationale"
    - path: "templates/constraint.md"
      provides: "Constraint template with Source, Requirements, Impact on Code, Verification"
      contains: "## Impact on Code"
    - path: "templates/agents-snippet.md"
      provides: "Static AGENTS.md snippet with sentinel comments, Project Context and Confidential Context sections"
      contains: "<!-- domain-context:start -->"
    - path: "templates/architecture.md"
      provides: "ARCHITECTURE.md template with System Purpose, Module Map, Data Flow, Key Boundaries, Technology Decisions"
      contains: "## Module Map"
    - path: "templates/claude.md"
      provides: "Thin CLAUDE.md pointer file"
      contains: "@AGENTS.md"
  key_links:
    - from: "templates/agents-snippet.md"
      to: "dc:init (Phase 2)"
      via: "sentinel comments for idempotent injection"
      pattern: "<!-- domain-context:(start|end) -->"
    - from: "templates/manifest.md"
      to: "dc:init (Phase 2)"
      via: "placeholder replacement"
      pattern: "\\{one_line_description\\}"
---

<objective>
Create all 8 template files in the templates/ directory, matching the Domain Context spec exactly.

Purpose: Templates are the source-of-truth scaffolding that dc:init and other skills use to generate .context/ files in target projects. Every downstream skill depends on these templates existing and being spec-compliant.

Output: 8 markdown template files in templates/ ready for consumption by dc:init (Phase 2).
</objective>

<execution_context>
@/Users/alevine/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-templates/01-CONTEXT.md
@.planning/phases/01-templates/01-RESEARCH.md
@~/code/domain-context/SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create structural templates (manifest, agents-snippet, architecture, claude)</name>
  <files>templates/manifest.md, templates/agents-snippet.md, templates/architecture.md, templates/claude.md</files>
  <action>
Create the templates/ directory and 4 structural template files. Use the exact content specified in the RESEARCH.md "Required Sections Per Template" section, which was transcribed directly from the spec.

**templates/manifest.md** -- Spec Section 6.3:
- Title: `# Context Manifest` with blockquote `> {one_line_description}`
- Sections: Access Levels (static boilerplate), Domain Concepts, Architecture Decisions, Constraints, Module Context Files (all starting with "(none yet)"), If Restricted Context Is Unavailable
- Placeholders: `{one_line_description}`, `{restricted_context_instructions}`
- Access Levels content is STATIC (copied verbatim from spec) -- do NOT add placeholders there
- Add one-line HTML guidance comments above each placeholder

**templates/agents-snippet.md** -- Spec Section 6.1:
- Wrap in `<!-- domain-context:start -->` and `<!-- domain-context:end -->` sentinel comments
- Contains "## Project Context" section with Domain Context link, @ARCHITECTURE.md, @.context/MANIFEST.md, per-module CONTEXT.md references, usage instructions
- Contains "## Confidential Context" section with .context.local/ instructions
- OMIT the sync-context.sh reference line (user decision: no sync script template)
- Entirely STATIC -- no placeholders

**templates/architecture.md** -- Spec Section 6.2:
- Sections: System Purpose, Module Map (with table template), Data Flow, Key Boundaries, Technology Decisions
- Placeholders: `{system_purpose}`, `{module_rows}`, `{data_flow}`, `{key_boundaries}`, `{technology_decisions}`
- Include the Module Map table header row as static content

**templates/claude.md** -- ADR-002 thin pointer:
- Content: `@AGENTS.md` (single line)
- STATIC -- no placeholders

CRITICAL: Copy section headings character-for-character from the spec. Do NOT paraphrase. Cross-reference the spec at ~/code/domain-context/SPEC.md Section 6 to verify exact heading text.
  </action>
  <verify>
    <automated>ls templates/manifest.md templates/agents-snippet.md templates/architecture.md templates/claude.md && grep -l '## Domain Concepts' templates/manifest.md && grep -l 'domain-context:start' templates/agents-snippet.md && grep -l '## Module Map' templates/architecture.md && grep -l '@AGENTS.md' templates/claude.md</automated>
  </verify>
  <done>4 structural template files exist with correct section headings matching the spec. manifest.md has all 6 sections, agents-snippet.md has sentinel comments and both Project/Confidential Context sections, architecture.md has all 5 sections, claude.md has the @AGENTS.md pointer.</done>
</task>

<task type="auto">
  <name>Task 2: Create entry templates (context, domain-concept, decision, constraint)</name>
  <files>templates/context.md, templates/domain-concept.md, templates/decision.md, templates/constraint.md</files>
  <action>
Create 4 entry template files for individual context entries. Use the exact content from RESEARCH.md "Required Sections Per Template" section.

**templates/context.md** -- Module CONTEXT.md, Spec Section 6.7:
- Title: `# {module_name}` followed by `<!-- verified: {verified_date} -->`
- Sections: What This Module Does, Domain Concepts Owned, Business Rules, Non-Obvious Decisions, What This Module Does NOT Do (note: capitalized NOT), Dependencies
- Placeholders: `{module_name}`, `{verified_date}`, `{module_description}`, `{domain_concepts_owned}`, `{business_rules}`, `{non_obvious_decisions}`, `{module_exclusions}`, `{dependencies}`

**templates/domain-concept.md** -- Spec Section 6.4:
- Title: `# {concept_name}` followed by `<!-- verified: {verified_date} -->`
- Sections: What This Is, Lifecycle, Key Attributes, Business Rules, Invariants, Related Concepts
- Placeholders: `{concept_name}`, `{verified_date}`, `{concept_description}`, `{lifecycle}`, `{key_attributes}`, `{business_rules}`, `{invariants}`, `{related_concepts}`

**templates/decision.md** -- Spec Section 6.5:
- Title: `# ADR-{number}: {decision_title}` followed by `<!-- verified: {verified_date} -->`
- Sections: Status, Context, Decision, Rationale, Consequences, Affected Modules
- Placeholders: `{number}`, `{decision_title}`, `{verified_date}`, `{status}`, `{context}`, `{decision}`, `{rationale}`, `{consequences}`, `{affected_modules}`

**templates/constraint.md** -- Spec Section 6.6:
- Title: `# {constraint_area}` followed by `<!-- verified: {verified_date} -->`
- Sections: Source, Requirements, Impact on Code, Verification
- Placeholders: `{constraint_area}`, `{verified_date}`, `{source}`, `{requirements}`, `{impact_on_code}`, `{verification}`

For ALL 4 files:
- Include `<!-- verified: {verified_date} -->` on the line immediately after the title
- Add one-line HTML guidance comments BELOW each heading, ABOVE the placeholder (e.g., `<!-- 2-4 sentences: what this concept is and its role -->`)
- Include ALL sections, even situational ones (e.g., Lifecycle in domain-concept.md) per user decision
- Use single curly braces and snake_case for placeholder names
- Cross-reference ~/code/domain-context/SPEC.md Section 6 for exact heading text
  </action>
  <verify>
    <automated>ls templates/context.md templates/domain-concept.md templates/decision.md templates/constraint.md && grep -c '{' templates/context.md templates/domain-concept.md templates/decision.md templates/constraint.md && grep -l 'What This Module Does NOT Do' templates/context.md && grep -l '## Invariants' templates/domain-concept.md && grep -l '## Rationale' templates/decision.md && grep -l '## Impact on Code' templates/constraint.md</automated>
  </verify>
  <done>4 entry template files exist. Each has the verified date HTML comment, all spec-required sections with correct heading text, {placeholder} tokens for dynamic content, and brief HTML guidance comments. Total template count: 8 files in templates/.</done>
</task>

</tasks>

<verification>
All 8 template files exist in templates/:
- `ls templates/*.md | wc -l` returns 8
- Each file's section headings match the spec (validated by Plan 02's validation script)
- Placeholder tokens follow `{snake_case}` pattern
- No double-curly-brace patterns exist: `grep '{{' templates/*.md` returns nothing
- Static content (Access Levels, agents snippet) contains no `{placeholder}` tokens
</verification>

<success_criteria>
- 8 template files exist: manifest.md, context.md, domain-concept.md, decision.md, constraint.md, agents-snippet.md, architecture.md, claude.md
- Section headings match the Domain Context spec verbatim
- Dynamic content uses `{placeholder}` tokens with snake_case names
- Static content has no placeholders
- AGENTS.md snippet wrapped in sentinel comments
- HTML guidance comments are brief (one line) and placed below headings
</success_criteria>

<output>
After completion, create `.planning/phases/01-templates/01-01-SUMMARY.md`
</output>
