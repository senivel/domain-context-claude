---
phase: 19-readme-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05

must_haves:
  truths:
    - "New user can copy-paste a one-line install command from the README"
    - "New user can follow a 3-step quick start to get domain-context working"
    - "User can look up any dc:* command and understand what it does and when to use it"
    - "GSD user understands how domain-context integrates without configuration"
    - "User can find uninstall instructions for both global and local installs"
  artifacts:
    - path: "README.md"
      provides: "Complete project documentation"
      contains: "npx domain-context-cc"
    - path: "LICENSE"
      provides: "MIT license file for badge link"
      contains: "MIT License"
  key_links:
    - from: "README.md"
      to: "https://github.com/senivel/domain-context"
      via: "spec link in intro"
      pattern: "github.com/senivel/domain-context"
    - from: "README.md"
      to: "https://www.npmjs.com/package/domain-context-cc"
      via: "npm badge link"
      pattern: "npmjs.com/package/domain-context-cc"
---

<objective>
Rewrite README.md to production quality and create LICENSE file so a new user can discover, install, and start using domain-context-cc from the README alone.

Purpose: This is the final phase before publishing. The README is the primary entry point for all users.
Output: Production-ready README.md with badges, prerequisites, per-command paragraphs, complete uninstall docs; MIT LICENSE file.
</objective>

<execution_context>
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alevine/code/domain-context-claude/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-readme-publishing/19-CONTEXT.md
@.planning/phases/19-readme-publishing/19-RESEARCH.md

Source files for command descriptions:
@commands/dc/init.md
@commands/dc/explore.md
@commands/dc/validate.md
@commands/dc/add.md
@commands/dc/refresh.md
@commands/dc/extract.md

Source files for install behavior:
@bin/install.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MIT LICENSE file</name>
  <files>LICENSE</files>
  <action>Create a standard MIT License file at the repo root. Use "2026 Senivel" as the copyright holder (matches the GitHub org from package.json repository URL). Use the standard MIT license text verbatim.</action>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE && grep -q "2026" LICENSE && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>LICENSE file exists at repo root with MIT license text and correct copyright year/holder.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md to production quality</name>
  <files>README.md</files>
  <action>
Rewrite README.md preserving the existing structure but expanding it per user decisions. Read each commands/dc/*.md file to extract the `description` field from YAML frontmatter as the source of truth for command paragraphs.

Structure (top to bottom):

1. **Badges** (NEW): Two shields.io badges on one line — npm version and license.
   ```
   [![npm version](https://img.shields.io/npm/v/domain-context-cc)](https://www.npmjs.com/package/domain-context-cc)
   [![license](https://img.shields.io/npm/l/domain-context-cc)](https://github.com/senivel/domain-context-claude/blob/main/LICENSE)
   ```

2. **Title + intro**: Keep existing title "# Domain Context for Claude Code" and one-liner blockquote. Keep the existing intro paragraph but ensure it links to the spec repo. Add a one-line prerequisites note: "Requires Node.js 20+ and Claude Code."

3. **Installation**: Keep existing 3-command block (global, local, uninstall). No changes needed.

4. **Quick Start**: Keep existing 3-step list. No changes needed.

5. **Commands**: Keep existing table as-is. Below the table, add one paragraph per command. Each paragraph starts with bold command name, then describes what it does and when to use it. Source the descriptions from each skill's YAML frontmatter `description` field. Per user decision: no usage examples, only document flags/options where they exist (dc:init has GSD detection), flat list, no grouping.

6. **GSD Integration**: Keep existing section as-is. It already has the 4-bullet bridge pattern explanation. Verify it is accurate and complete.

7. **What Gets Installed**: Keep existing section as-is. No changes needed per user decision.

8. **Uninstall** (NEW section, split from Installation): Create a dedicated "Uninstall" section after "What Gets Installed" documenting both global and local uninstall:
   ```bash
   # Global uninstall
   npx domain-context-cc --uninstall

   # Local uninstall
   npx domain-context-cc --local --uninstall
   ```
   Add one sentence explaining what uninstall does: removes dc-prefixed files and dc hook entries from settings.json, leaving other hooks intact.

9. **Links**: Keep existing section as-is.

IMPORTANT constraints per user decisions:
- NO "Why" or "Philosophy" section
- NO "How It Works" section
- NO usage examples for commands
- Concise, practical tone — short sentences, action-oriented, no marketing language
- Keep the uninstall commands in the Installation block too (they are already there)
  </action>
  <verify>
    <automated>grep -q 'shields.io/npm/v/domain-context-cc' README.md && grep -q 'shields.io/npm/l/domain-context-cc' README.md && grep -q 'Node.js 20' README.md && grep -q 'Quick Start' README.md && grep -q 'dc:init' README.md && grep -q 'dc:explore' README.md && grep -q 'dc:validate' README.md && grep -q 'dc:add' README.md && grep -q 'dc:refresh' README.md && grep -q 'dc:extract' README.md && grep -q 'GSD Integration' README.md && grep -q '\-\-uninstall' README.md && grep -q '\-\-local --uninstall' README.md && echo "ALL CHECKS PASS" || echo "SOME CHECKS FAIL"</automated>
  </verify>
  <done>README.md has: badges (npm version + license), prerequisites line, install commands, 3-step quick start, command table with per-command paragraphs for all 6 skills, GSD integration section, What Gets Installed section, dedicated uninstall section with both global and local commands, and links section. All grep checks pass.</done>
</task>

</tasks>

<verification>
All five DOC requirements verified via grep:
1. DOC-01: `grep -q 'npx domain-context-cc' README.md`
2. DOC-02: `grep -q 'Quick Start' README.md`
3. DOC-03: All 6 dc:* command names present in README
4. DOC-04: `grep -q 'GSD Integration' README.md`
5. DOC-05: `grep -q '\-\-uninstall' README.md`
</verification>

<success_criteria>
- README.md contains badges, prerequisites, install commands, quick start, command reference with per-command paragraphs, GSD integration, What Gets Installed, uninstall section, and links
- LICENSE file exists with MIT license text
- All 5 DOC requirement grep checks pass
- No "Why", "Philosophy", or "How It Works" sections exist
- Command descriptions match skill frontmatter (not invented)
</success_criteria>

<output>
After completion, create `.planning/phases/19-readme-publishing/19-01-SUMMARY.md`
</output>
